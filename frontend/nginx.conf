server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: qualquer rota sem arquivo correspondente cai no index.html (client-side routing).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
